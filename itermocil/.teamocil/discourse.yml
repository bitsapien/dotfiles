windows:
  - name: discourse
    root: ~/mysites/discourse
    layout: main-vertical
    panes:
      - commands:
        - vim .
      - commands:
        - vagrant up
        - vagrant ssh -c 'rails s -b 0.0.0.0'
      - commands:
        - vagrant ssh

